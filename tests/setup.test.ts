import { describe, it, expect } from "vitest";

// Probni test: jedini cilj mu je da potvrdi da je test okruzenje ispravno postavljeno.
// Brise se cim M1 donese prve prave testove parsera.
describe("postavka projekta", () => {
  it("test runner radi", () => {
    expect(1 + 1).toBe(2);
  });
});
