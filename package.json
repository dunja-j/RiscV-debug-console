{
  "name": "riscv-debug-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Editor, asembler i simulator za podskup RISC-V instrukcija",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
